function MainInput({label="", variant="outlined", defaultValue=""}) {
    return ( 

        <TextField label={label} variant={variant} defaultValue={defaultValue} />

     );
}

export default MainInput;